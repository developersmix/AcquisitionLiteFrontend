export class ActivitySerialized {

    // Activity
    activityId?: number
    date?: string;
    status?: number;
    time?: string;

    // Tracking
    trackingId?: number;

    // User
    userId?: number
    userOffice?: string;
}